# Claude Code entry point

Canonical agent guide lives in `AGENTS.md` (cross-agent standard). This file imports it
so Claude Code loads the same content — do not duplicate; edit `AGENTS.md`.

@AGENTS.md
